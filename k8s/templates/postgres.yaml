# Auto-generated from k8s/templates/postgres.yaml
# Edit project.yaml and run 'make generate-k8s' to regenerate. Do not edit by hand.
---
# CloudNativePG Cluster for the gRPC backend service.
# Apply after the CNPG operator is installed (scripts/install-cnpg.sh).
#
# CNPG automatically creates a secret named "${PROJECT_NAME}-db-app" in the
# ${NAMESPACE} namespace containing the connection URI and credentials.
# The backend deployment references this secret for DATABASE_URL.
apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: ${PROJECT_NAME}-db
  namespace: ${NAMESPACE}
spec:
  instances: 1

  storage:
    storageClass: local-path
    size: 5Gi

  bootstrap:
    initdb:
      database: ${PROJECT_NAME}
      owner: ${PROJECT_NAME}

  # Resource limits appropriate for a home lab node.
  resources:
    requests:
      cpu: 100m
      memory: 256Mi
    limits:
      cpu: 500m
      memory: 512Mi
