# Auto-generated from k8s/templates/web-deployment.yaml
# Edit project.yaml and run 'make generate-k8s' to regenerate. Do not edit by hand.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: ${PROJECT_NAME}-web
  namespace: ${NAMESPACE}
  labels:
    app: ${PROJECT_NAME}-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ${PROJECT_NAME}-web
  template:
    metadata:
      labels:
        app: ${PROJECT_NAME}-web
    spec:
      containers:
        - name: ${PROJECT_NAME}-web
          image: ${WEB_IMAGE}:latest
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            requests:
              cpu: 25m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 64Mi
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 10

---
# NodePort for direct LAN access
apiVersion: v1
kind: Service
metadata:
  name: ${PROJECT_NAME}-web
  namespace: ${NAMESPACE}
  labels:
    app: ${PROJECT_NAME}-web
spec:
  type: NodePort
  selector:
    app: ${PROJECT_NAME}-web
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: ${WEB_NODEPORT}
      protocol: TCP

---
# ClusterIP targeted by the cloudflared tunnel.
# In the Cloudflare Zero Trust dashboard, set the tunnel's public hostname to route
# to http://${PROJECT_NAME}-service.${NAMESPACE}:80 (k8s internal DNS).
apiVersion: v1
kind: Service
metadata:
  name: ${PROJECT_NAME}-service
  namespace: ${NAMESPACE}
  labels:
    app: ${PROJECT_NAME}-web
spec:
  type: ClusterIP
  selector:
    app: ${PROJECT_NAME}-web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
