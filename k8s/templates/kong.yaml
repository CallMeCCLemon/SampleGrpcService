# Auto-generated from k8s/templates/kong.yaml
# Edit project.yaml and run 'make generate-k8s' to regenerate. Do not edit by hand.
---
# Tells Kong to use the grpc-gateway plugin on routes that match the Ingress
# below, transcoding HTTP/JSON ↔ gRPC using the mounted proto definition.
apiVersion: configuration.konghq.com/v1
kind: KongPlugin
metadata:
  name: grpc-gateway
  namespace: ${NAMESPACE}
plugin: grpc-gateway
config:
  proto: /etc/kong/protos/${PROJECT_NAME}.proto

---
# Routes inbound HTTP traffic from Kong's proxy to the gRPC service.
# Kong transcodes each request using the grpc-gateway plugin.
#
# Endpoints exposed to the web app (defined by google.api.http annotations in the proto):
#   POST http://<node-ip>:${KONG_NODEPORT}${API_PREFIX}/echo  {"message":"hello world"}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ${PROJECT_NAME}-http
  namespace: ${NAMESPACE}
  annotations:
    konghq.com/plugins: grpc-gateway
    # Tell Kong this Ingress accepts plain HTTP from clients (the plugin handles
    # the gRPC conversion upstream; see konghq.com/protocol on the Service).
    konghq.com/protocols: http
    # DO NOT strip the prefix. Kong's grpc-gateway plugin matches the ORIGINAL
    # request path (before any strip-path rewrite) against the proto HTTP
    # annotations, so the proto must include the full api_prefix. The
    # `make check-api-paths` target guards this invariant.
    konghq.com/strip-path: "false"
spec:
  ingressClassName: kong
  rules:
    - http:
        paths:
          - path: ${API_PREFIX}
            pathType: Prefix
            backend:
              service:
                name: ${PROJECT_NAME}
                port:
                  number: 50051
