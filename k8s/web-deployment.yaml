# Auto-generated from k8s/templates/web-deployment.yaml
# Edit project.yaml and run 'make generate-k8s' to regenerate. Do not edit by hand.
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: greeter-web
  namespace: grpc-demo
  labels:
    app: greeter-web
spec:
  replicas: 1
  selector:
    matchLabels:
      app: greeter-web
  template:
    metadata:
      labels:
        app: greeter-web
    spec:
      containers:
        - name: greeter-web
          image: 192.168.1.110:32000/sample-grpc-web:0.2.2-0806a84
          imagePullPolicy: Always
          ports:
            - name: http
              containerPort: 80
              protocol: TCP
          resources:
            requests:
              cpu: 25m
              memory: 16Mi
            limits:
              cpu: 100m
              memory: 64Mi
          livenessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 5
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /
              port: 80
            initialDelaySeconds: 3
            periodSeconds: 10

---
# NodePort for direct LAN access
apiVersion: v1
kind: Service
metadata:
  name: greeter-web
  namespace: grpc-demo
  labels:
    app: greeter-web
spec:
  type: NodePort
  selector:
    app: greeter-web
  ports:
    - name: http
      port: 80
      targetPort: http
      nodePort: 30090
      protocol: TCP

---
# ClusterIP targeted by the cloudflared tunnel.
# In the Cloudflare Zero Trust dashboard, set the tunnel's public hostname to route
# to http://greeter-service.grpc-demo:80 (k8s internal DNS).
apiVersion: v1
kind: Service
metadata:
  name: greeter-service
  namespace: grpc-demo
  labels:
    app: greeter-web
spec:
  type: ClusterIP
  selector:
    app: greeter-web
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
